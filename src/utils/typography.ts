import Typography from 'typography';
const kirkhamTheme = require('typography-theme-kirkham').default;

const typography = new Typography(kirkhamTheme);

export default typography
export const rhythm = typography.rhythm
